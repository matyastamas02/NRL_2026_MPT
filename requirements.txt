# Pinned to a known-good, mutually-compatible stack (wheels for Python 3.11-3.13).
# streamlit pinned to a pre-removal version: app.py uses use_container_width (x34),
# which newer Streamlit removed after 2025-12-31 -> would crash the deployed app.
streamlit==1.41.1
pandas==2.2.3
numpy==2.2.1
scikit-learn==1.6.0
scipy==1.15.1
matplotlib==3.10.0
plotly==5.24.1
openpyxl==3.1.5
gspread==6.1.4
google-auth==2.37.0
