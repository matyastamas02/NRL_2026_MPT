# Pinned: translation_model_v2.pkl is unpickled at import time in
# predict_translation.py, so a scikit-learn major bump can break the app on
# startup rather than at use. Re-pin deliberately, and refit the pickle when you do.
streamlit>=1.28,<2
pandas>=2.0,<3
numpy>=1.24,<3
plotly>=5.18,<7
scikit-learn==1.7.2
